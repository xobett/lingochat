services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespw
      POSTGRES_DB: lingochat
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql
volumes:
  postgres_data: